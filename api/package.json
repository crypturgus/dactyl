{
  "scripts": {
    "build": "bash vercel_migrate.sh ; gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "start": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app"
  }
}
