{
  "scripts": {
    "build": "bash vercel_migrate.sh",
    "preview": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "run": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "start": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "dev": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "prod": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app",
    "serve": "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.vercel_wsgi:app"
  }
}
